import React from 'react';

const App = (): JSX.Element => {
    return (
        <div>
            learn react
            <button onClick={() => alert('ssssshelssslo')}>Click 22Me</button>
        </div>
    );
}

export default App;